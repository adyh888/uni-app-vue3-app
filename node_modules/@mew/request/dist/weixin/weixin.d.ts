import { AxiosInstance } from "axios";
export declare class WeiXin {
    _appId: string;
    _service: AxiosInstance;
    _access_token: string;
    constructor(appId: string, service: AxiosInstance);
    private init;
    private getMsAccessToken;
    /**
     * 仅支持在微信小程序内调用才有效
     */
    getUserInfo(code: string): Promise<import("../request").IRes<import("../ms/mc").getAppUserInfoData>>;
    sendTemplateMessage(touser: any, data: any): Promise<any>;
}
