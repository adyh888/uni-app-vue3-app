import { IResType } from "../request";
export interface RabbitClient {
    node: string;
    serviceName: string;
}
export declare function Rabbit(service: any, isDebugMode?: boolean): {
    getClients(): Promise<IResType<RabbitClient>>;
    getNodes(): Promise<IResType<RabbitClient>>;
};
