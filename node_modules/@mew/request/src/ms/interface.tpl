export interface ${MainName} {
    id: number
    startAt?: number
    endAt?: number
}

export interface ${MainName}Property {
    id?: number
}

export interface ${MainName}SelectProperty extends ${MainName}Property {
    curPageNo?: number
    pageSize?: number
    startAt?: number
    endAt?: number
}

export interface ${MainName}UpdateProperty extends ${MainName}Property {
    id: number
}

export interface ${MainName}InsertProperty extends ${MainName}Property {

}

export interface ${MainName}DeleteProperty extends ${MainName}Property {
    id: number
}
